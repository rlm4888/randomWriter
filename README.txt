# Random Writer

Random Writer is a Java program that generates random text based on a given source text.

## Installation

1. Clone the repository: `git clone https://github.com/yourusername/random-writer.git`
2. Compile the Java files: `javac RandomWriter.java`

## Usage

Run the program with the following command:

```bash
java RandomWriter input.txt output.txt level characters
